 class script{
 
    static function(String){
        Seatwork.html != undefined ? getComputedStyle(h1, "font-weight", undefined)
         : getComputedStyle(h1, "font-weight", String.substring(   h1.lengt, h1.length)).setAttribute;
    }
}